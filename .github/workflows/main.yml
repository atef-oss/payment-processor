name: Payment Processor CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Print a message
        run: echo "CI/CD pipeline for payment-processor has started"
      - name: Build and test
        run: |
          echo "Building and testing payment-processor..."
          ls